

<h1>Todo List</h1>

<center>
<div class="card" style="width: 18rem;">
    <div class="card-body">
      <%= render 'form_index' %>
    </div>
  </div>
</center>
<br/><br/>

<table class="table table-bordered">
  

  <tbody>
    <% @lists.each do |list| %>
    
      <% if list.completed %>
      <tr class="table-secondary">
        <td class="striker"><%= link_to list.description, edit_list_path(list) %><% list.description %></td>
        <td>
        <% link_to 'Show', list %>
        Created <%= (list.created_at)%> ago
        <% link_to 'Edit', edit_list_path(list) %>
       <center><%= link_to 'Delete', list, method: :delete, data: { confirm: 'Are you sure?' } %></td></center>
      </tr>
      <% else %>
      <tr>
        <td><%= link_to list.description, edit_list_path(list) %><% list.description %></td>
        <td>
        <% link_to 'Show', list %>
        Created <%= (list.created_at)%> ago
        <% link_to 'Edit', edit_list_path(list) %>
        <center><%= link_to 'Delete', list,  method: :delete, data: { confirm: 'Are you sure?' } %></td></center>
      </tr>
      
      <% end %>
      <% end %>
  </tbody>
</table>

<br>
<p>Click on todo description to edit</p>

